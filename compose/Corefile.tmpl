.:53 {
  template IN A {
    match ^${DOMAIN_RE}\.$
    answer "{{ .Name }} 60 IN A ${IP4}"
    fallthrough
  }

  template IN A {
    match ^(.*)\.${DOMAIN_RE}\.$
    answer "{{ .Name }} 60 IN A ${IP4}"
    fallthrough
  }

  template IN AAAA {
    match ^${DOMAIN_RE}\.$
    rcode NXDOMAIN
  }

  template IN AAAA {
    match ^(.*)\.${DOMAIN_RE}\.$
    rcode NXDOMAIN
  }

  forward . /etc/resolv.conf
  log
  errors
}
