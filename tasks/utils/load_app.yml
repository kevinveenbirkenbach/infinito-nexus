- name: "Harden load_app_id"
  ansible.builtin.set_fact:
    _load_app_id: "{{ load_app_id }}"
  changed_when: false

# Load with load app id
- name: "Caller '{{ application_id }}' loads application: '{{ _load_app_id }}'"
  include_role:
    name:   "{{ _load_app_id }}"
    public: false
  vars:
    application_id: "{{ _load_app_id }}"
    domain:         "{{ domains | get_domain(load_app_id) }}"
