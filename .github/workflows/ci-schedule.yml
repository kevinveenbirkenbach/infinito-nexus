name: CI (Scheduled)

concurrency:
  group: ci-schedule-global
  cancel-in-progress: false

on:
  schedule:
    - cron: "33 10 * * 2"

permissions:
  contents: read
  packages: write
  security-events: write

jobs:
  ci-schedule:
    uses: ./.github/workflows/code-analyses.yml
