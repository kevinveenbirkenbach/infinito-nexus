name: CI (Scheduled)

on:
  schedule:
    - cron: "33 10 * * 2"

permissions:
  contents: read
  packages: write
  security-events: write

jobs:
  ci-schedule:
    uses: ./.github/workflows/code-analyses.yml
