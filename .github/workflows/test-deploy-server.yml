name: Test Infinito.Nexus Deploy (server)

on:
  workflow_call:

permissions:
  contents: read
  packages: read

jobs:
  call:
    uses: ./.github/workflows/test-deploy.yml
    with:
      deploy_type: server
    secrets: inherit
