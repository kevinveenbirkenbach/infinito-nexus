name: CI (push)

on:
  push:
    branches:
      - main
      - master
    tags-ignore:
      - "v*"
      - "stable"

permissions:
  contents: read
  packages: read
  security-events: write

jobs:
  code-analyses:
    uses: ./.github/workflows/code-analyses.yml
