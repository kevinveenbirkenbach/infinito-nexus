name: CI (push)

on:
  push:
    tags-ignore:
      - "v*"
      - "stable"
      - "latest"

permissions:
  contents: read
  packages: read
  security-events: write

jobs:
  code-analyses:
    uses: ./.github/workflows/code-analyses.yml
