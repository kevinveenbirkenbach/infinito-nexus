---
ssh:
  lifecycle: pre-alpha
  # Repository URL containing the user's SSH key material; override in the
  # inventory under `applications.desk-ssh.services.ssh.repository`.
  repository: ""
  min_storage: "50MB"
  cpus: "0.1"
  mem_reservation: "32m"
  mem_limit: "64m"
  pids_limit: 64
