WORDPRESS_DB_HOST=      "{{ database_host }}:{{ database_port }}"
WORDPRESS_DB_USER=      "{{ database_username }}"
WORDPRESS_DB_PASSWORD=  {{ database_password | dotenv_quote }}
WORDPRESS_DB_NAME=      "{{ database_name }}"

# Debug flags
WP_DEBUG={{ MODE_DEBUG | lower }}
WP_DEBUG_LOG={{ MODE_DEBUG | lower }}
WP_DEBUG_DISPLAY={{ MODE_DEBUG | lower }}
