postgres_gis_enabled:   false               # Needed by mobilizon
database_init:          false               # When true a database is initialized
database_consumer_id:   "svc-db-postgres"   # Self consumer
