- block:

  - name: Include dependency 'sys-alm-compose'
    include_role:
      name: sys-alm-compose
    when: run_once_sys_alm_compose is not defined

  - include_tasks: utils/run_once.yml
  when: run_once_sys_hlth_btrfs is not defined

- name: "create {{docker_health_btrfs_folder}}"
  file:
    path: "{{docker_health_btrfs_folder}}"
    state: directory
    mode: "0755"

- name: create sys-hlth-btrfs.sh
  copy:
    src: sys-hlth-btrfs.sh
    dest: "{{docker_health_btrfs_folder}}sys-hlth-btrfs.sh"

- name: create sys-hlth-btrfs{{ SYS_SERVICE_SUFFIX }}
  template:
    src: sys-hlth-btrfs.service.j2
    dest: /etc/systemd/system/sys-hlth-btrfs{{ SYS_SERVICE_SUFFIX }}
  notify: reload sys-hlth-btrfs service

- name: "set 'service_name' to '{{ role_name }}'"
  set_fact:
    service_name: "{{ role_name }}"

- name: "include role for sys-timer for {{service_name}}"
  include_role:
    name: sys-timer
  vars:
    on_calendar: "{{on_calendar_health_btrfs}}"
