features:
  matomo:           true
  css:              true
  desktop:          true
  central_database: true
  logout:           true
docker:
  services:
    redis:
      enabled: true
    database: 
      enabled: true
    web:              
      image:    "attendize_web"
      version:  "latest"
    worker:           
      image:    "attendize_worker"
      version:  "latest"
server:
  domains:
    canonical:
      - "tickets.{{ PRIMARY_DOMAIN }}"
