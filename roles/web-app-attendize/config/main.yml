image:              
  web:              "attendize_web:latest"
  worker:           "attendize_worker:latest"
features:
  matomo:           true
  css:              true
  desktop:          true
  central_database: true
  logout:           true
docker:
  services:
    redis:
      enabled: true
    database: 
      enabled: true
server:
  domains:
    canonical:
      - "tickets.{{ PRIMARY_DOMAIN }}"
