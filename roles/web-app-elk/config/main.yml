server:
  domains:
    canonical:
      - elk.{{ DOMAIN_PRIMARY }}
    aliases: []
docker:
  services:
    logout:
      enabled: false
      shared: false
    matomo:
      enabled: true
      shared: true
