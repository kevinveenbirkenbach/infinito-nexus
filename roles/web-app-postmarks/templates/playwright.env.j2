APP_BASE_URL={{ lookup('tls', application_id, 'url.base') }}
POSTMARKS_BASE_URL={{ lookup('tls', application_id, 'url.base') | dotenv_quote }}
CANONICAL_DOMAIN={{ (lookup('applications', application_id).server.domains.canonical | first) | dotenv_quote }}

# Service gate flags (requirement 006). Postmarks supports OIDC and
# LDAP via a sidecar oauth2-proxy in front of the Postmarks web UI;
# in-app RBAC is a documented exception per requirement 013 and
# lifecycle.md (see README.md).
OIDC_SERVICE_ENABLED={{ lookup('config', application_id, 'services.oauth2.enabled') | string | lower }}
LDAP_SERVICE_ENABLED={{ lookup('config', application_id, 'services.oauth2.enabled') | string | lower }}

NODE_TLS_REJECT_UNAUTHORIZED=0
