---
- name: "load docker, proxy for '{{ application_id }}'"
  include_role:
    name: sys-stk-full
  vars:
    docker_compose_flush_handlers: true
    compose_repository_url: "{{ ROULETTE_REPOSITORY }}"
    compose_repository_pull: true
    compose_repository_ref: "{{ ROULETTE_REF }}"
