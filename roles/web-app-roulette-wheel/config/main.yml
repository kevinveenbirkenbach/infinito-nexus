features:
  css:      true
server:
  domains:
    canonical:
      - "wheel.{{ DOMAIN_PRIMARY }}"
    aliases: []
docker:
  services:
    logout:
      enabled: false
      shared: false
    desktop:
      enabled: true
    matomo:
      enabled: true
      shared: true
