server:
  domains:
    canonical:
      - "wheel.{{ DOMAIN_PRIMARY }}"
    aliases: []
docker:
  services:
    logout:
      enabled: false
      shared: false
    desktop:
      enabled: true
    matomo:
      enabled: true
      shared: true
    css:
      enabled: true
    roulette-wheel:
      cpus:               0.5
      mem_reservation:    126m
      mem_limit:          265m
      pids_limit:         126
