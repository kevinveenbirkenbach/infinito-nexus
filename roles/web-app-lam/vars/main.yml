# General
application_id: "web-app-lam"

# LAM specific
LAM_IMAGE:      "{{ applications | get_app_conf(application_id, 'docker.services.lam.image') }}"
LAM_CONTAINER:  "{{ application_id | get_entity_name }}"
LAM_PASSWORD:   "{{ applications | get_app_conf(application_id, 'credentials.administrator_password') }}"
