# General
application_id: "web-app-lam"

# LAM specific
LAM_IMAGE:      "{{ lookup('config', application_id, 'compose.services.lam.image') }}"
LAM_CONTAINER:  "{{ application_id | get_entity_name }}"
LAM_PASSWORD:   "{{ lookup('config', application_id, 'credentials.administrator_password') }}"
