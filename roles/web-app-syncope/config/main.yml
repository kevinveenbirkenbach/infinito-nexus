server:
  domains:
    canonical:
      - syncope.{{ DOMAIN_PRIMARY }}
    aliases: []
docker:
  services:
    logout:
      enabled: false
      shared: false
    matomo:
      enabled: true
      shared: true
