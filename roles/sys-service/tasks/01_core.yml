- name: Include dependency 'sys-daemon'
  include_role:
    name: sys-daemon
  when: run_once_sys_daemon is not defined

- name: "Stop services and lock timers"
  include_tasks: 02_lockdown.yml

- name: "reset (if enabled) for {{ role_name}}"
  include_tasks: 03_reset.yml
  when: MODE_RESET | bool

- include_tasks: utils/once/flag.yml
