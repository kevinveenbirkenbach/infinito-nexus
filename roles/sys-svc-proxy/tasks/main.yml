- block:
  - name: Include dependencies
    include_role:
      name: '{{ item }}'
    loop:
    - sys-stk-front-pure
    - sys-svc-webserver
  - include_tasks: utils/run_once.yml
  when: run_once_sys_svc_proxy is not defined
