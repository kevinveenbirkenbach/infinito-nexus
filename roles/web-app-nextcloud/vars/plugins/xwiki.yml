plugin_configuration:
  - appid: "xwiki"
    configkey: "instances"
    configvalue:
      - name:  "XWiki"
        url:   "{{ domains | get_url('web-app-xwiki', applications | tls_web_protocol('web-app-xwiki', TLS_ENABLED)) }}"
