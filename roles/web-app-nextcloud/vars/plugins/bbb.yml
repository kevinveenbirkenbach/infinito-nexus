plugin_configuration:
  - appid:        "bbb"
    configkey:    "api.secret"
    configvalue:  "{{ lookup('config','web-app-bigbluebutton', 'credentials.shared_secret', '') }}"
  - appid:        "bbb"
    configkey:    "api.url"
    configvalue:  "{{ lookup('tls', 'web-app-bigbluebutton', 'url.base') }}{{ lookup('config','web-app-bigbluebutton', 'api_suffix','') }}"
