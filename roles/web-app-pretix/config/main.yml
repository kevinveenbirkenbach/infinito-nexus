credentials: {}
docker:
  services:
    redis:
      enabled:      true
    database:
      enabled:      true
    pretix:
      image:        pretix/standalone
      version:      stable
      name:         pretix
  volumes:
    data:           "pretix_data"
features:
  matomo:           true
  css:              true
  desktop:          true
  central_database: true
  logout:           true
  oidc:             true
server:
  csp:
    whitelist:      {}
    flags:          {}
  domains:
    canonical:
      - "ticket.{{ PRIMARY_DOMAIN }}"
    aliases:
      - "pretix.{{ PRIMARY_DOMAIN }}"
rbac:
  roles: {}
plugins:
  oidc:
    version: "2.3.1"

