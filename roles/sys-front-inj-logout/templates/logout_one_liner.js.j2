document.addEventListener('DOMContentLoaded', function() {
    initLogoutPatch(
        '{{ OIDC.CLIENT.LOGOUT_URL }}',
        '{{ WEB_PROTOCOL }}',
        '{{ DOMAIN_PRIMARY }}',
        {{ MODE_DEBUG | lower }}
    );
});
