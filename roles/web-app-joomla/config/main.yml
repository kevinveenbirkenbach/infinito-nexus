autocreate_users:     true # Autocreate LDAP users on Login
features:
  matomo:             true
  css:                true
  desktop:            true
  central_database:   true
  logout:             true
  ldap:               true
server:
  domains:
    canonical:
      - "cms.{{ PRIMARY_DOMAIN }}"
  csp:
    flags:
      style-src:
        unsafe-inline: true
      script-src-elem:
        unsafe-inline: true
docker:
  services:
    database:
      enabled:  true
    joomla: 
      image:    joomla
      version:  latest
      name:     joomla
  volumes:
    data:       "joomla_data"
