features:
server:
  domains:
    canonical:
      - jenkins.{{ DOMAIN_PRIMARY }}
    aliases: []
docker:
  services:
    logout:
      enabled: true
      shared: true
    matomo:
      enabled: true
      shared: true
