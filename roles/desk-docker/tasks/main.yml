- name: install docker
  package:
    name:
      - docker
      - docker-compose
    state: present

- name: "Adding user {{ users[WORKSTATION_USER].username }} to relevant docker usergroup"
  user: 
    name: "{{ users[WORKSTATION_USER].username }}"
    groups: docker
    append: yes
