server:
  domains:
    canonical:
      - "wiki.{{ PRIMARY_DOMAIN }}"
docker:
  services:
    mediawiki: 
      image:    mediawiki
      version:  latest
      backup:
        no_stop_required: true
      name:     mediawiki
    volumes:
      data:     mediawiki_data
features:
  logout: true
