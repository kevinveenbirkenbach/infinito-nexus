credentials: {}
docker:
  services:
    oidc:
      enabled: false
      shared: false
    logout:
      enabled: true

    desktop:
      enabled: true
    matomo:
      enabled: true
      shared: true
    database:
      shared: true
      type: "postgres"
      enabled:      true
    confluence:
      image:            atlassian/confluence
      version:          latest
      name:             confluence
      cpus:             "3.0"
      mem_reservation:  "6g"
      mem_limit:        "8g"
      pids_limit:       2048
    css:
      enabled: true
  volumes:
    data:           "confluence_data"
server:
  status_codes:
    default: 302
  csp:
    whitelist:      {}
    flags:
      script-src-elem:
        unsafe-inline:  true
      script-src-attr:
        unsafe-inline:  true
  domains:
    canonical:
      - "c.wiki.{{ DOMAIN_PRIMARY }}"
    aliases: []
rbac:
  roles: {}
truststore_enabled: false