ssl_certificate     {{ lookup('cert_plan', application_id, want='files.cert') }};
ssl_certificate_key {{ lookup('cert_plan', application_id, want='files.key') }};
{% if lookup('cert_plan', application_id, want='files.ca') | default('') | length > 0 %}
ssl_trusted_certificate {{ lookup('cert_plan', application_id, want='files.ca') }};
{% endif %}
