MYSQL_DATABASE="{{ lookup('database', application_id, want='name') }}"
MYSQL_USER="{{ lookup('database', application_id, want='username') }}"
MYSQL_PASSWORD="{{ lookup('database', application_id, want='password') }}"
MYSQL_ROOT_PASSWORD="{{ lookup('database', application_id, want='password') }}"
MARIADB_AUTO_UPGRADE="1"