- name: Include dependencies
  include_role:
    name: '{{ item }}'
  loop:
  - dev-python-pip
  - sys-ctl-alm-compose
  - sys-lock

- name: install lsof and python-psutil
  community.general.pacman:
    name:
    - lsof
    - python-psutil
    state: present

- include_role:
    name: sys-systemctl