- block:
    - name: update pacman
      community.general.pacman:
        update_cache: yes
        upgrade: yes

    - include_tasks: utils/once/flag.yml
  when: run_once_update_pacman is not defined