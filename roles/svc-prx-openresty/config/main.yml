compose:
  services:
    openresty:
      name:             "openresty"
      cpus:             0.5
      mem_reservation:  1g
      mem_limit:        2g
  volumes:
    www:    "/var/www/"
    nginx:  "/etc/nginx/"
