features:
  css:      true
server:
  domains:
    canonical:
      - "file.{{ DOMAIN_PRIMARY }}"
    aliases: []
docker:
  services:
    desktop:
      enabled: true
      shared: true
    matomo:
      enabled: true
      shared: true
