features:
  css:      true
server:
  domains:
    canonical:
      - "file.{{ DOMAIN_PRIMARY }}"
    aliases: []
docker:
  services:
    desktop:
      enabled: true
    matomo:
      enabled: true
      shared: true
