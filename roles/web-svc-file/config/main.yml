server:
  domains:
    canonical:
      - "file.{{ DOMAIN_PRIMARY }}"
    aliases: []
compose:
  services:
    dashboard:
      enabled: true
    matomo:
      enabled: true
      shared: true
    css:
      enabled: true
