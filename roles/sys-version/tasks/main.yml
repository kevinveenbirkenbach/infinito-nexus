- name: Ensure system version is set in environment file
  ansible.builtin.lineinfile:
    path: "{{ FILE_ENVIRONMENT }}"
    regexp: '^{{ INFINITO_VERSION_ENV_NAME }}='
    line: '{{ INFINITO_VERSION_ENV_NAME }}="{{ lookup(''version'') }}"'
    create: true
    mode: "0644"
    state: present
