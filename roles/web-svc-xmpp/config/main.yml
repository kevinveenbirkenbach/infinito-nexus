# xmpp is more a service then a app with ui interface. @todo Rename it 
server:
  domains:
    canonical:
      - xmpp.{{ DOMAIN_PRIMARY }}
    aliases: []
docker:
  services:
    logout:
      enabled: false
      shared: false
