features:
  css:      true
server:
  domains:
    canonical:
      - "html.{{ DOMAIN_PRIMARY }}"
    aliases: []
docker:
  services:
    desktop:
      enabled: true
    matomo:
      enabled: true
      shared: true
