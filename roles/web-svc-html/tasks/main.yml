- block:
  - name: Include dependencies
    include_role:
      name: '{{ item }}'
    loop:
    - sys-stk-front-proxy
    - dev-git
  - include_tasks: utils/once/flag.yml
  when: run_once_web_svc_html is not defined
