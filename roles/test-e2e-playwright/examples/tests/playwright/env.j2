# Base URL used by Playwright tests (available as process.env.APP_BASE_URL)
APP_BASE_URL={{ lookup('tls', application_id, 'url.base') }}

# Optional: credentials from the shared users map
# LOGIN_USERNAME={{ users.administrator.username | dotenv_quote }}
# LOGIN_PASSWORD={{ users.administrator.password | dotenv_quote }}
