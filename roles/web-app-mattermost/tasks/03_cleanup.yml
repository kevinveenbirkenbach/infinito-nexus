---
- name: "Mattermost | Cleanup — confirm OIDC state"
  debug:
    msg: >
      GitLab OAuth (OIDC) is {{ 'enabled' if MM_OIDC_ENABLED | bool else 'disabled' }}.
      MM_GITLABSETTINGS_ENABLE in env.j2 reflects this automatically on next deploy.
