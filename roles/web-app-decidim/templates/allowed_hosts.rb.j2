# frozen_string_literal: true
# Managed by Ansible - do not edit by hand
# Allows requests from the configured Decidim domain

Rails.application.config.hosts << "{{ lookup('tls', application_id, 'url.base') | regex_replace('https?://', '') | regex_replace('/+$', '') }}"
Rails.application.config.hosts << /.*\.{{ DOMAIN_PRIMARY | replace('.', '\\.') }}/
Rails.application.config.hosts << "localhost"
Rails.application.config.hosts << "127.0.0.1"
Rails.application.config.hosts << /\A\d+\.\d+\.\d+\.\d+\z/
