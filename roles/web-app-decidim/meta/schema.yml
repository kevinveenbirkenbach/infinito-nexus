credentials:
  secret_key_base:
    description: Rails secret key base for cryptographic operations in Decidim
    algorithm: sha256
    validation: ^[a-f0-9]{128}$
  database_password:
    description: Password for the Decidim PostgreSQL database user
    algorithm: bcrypt
    validation: ^\$2[aby]\$.{56}$
