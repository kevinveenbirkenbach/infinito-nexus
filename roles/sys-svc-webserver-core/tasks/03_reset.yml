- name: Delete NGINX config paths
  file:
    path: "{{ item }}"
    state: absent
  loop:
    - "{{ lookup('nginx_paths','directories.configuration.base') }}"
    - "{{ lookup('nginx_paths','files.configuration') }}"
  loop_control:
    label: "{{ item }}"
