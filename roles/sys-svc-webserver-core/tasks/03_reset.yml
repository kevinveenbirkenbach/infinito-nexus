- name: Delete NGINX config paths
  file:
    path: "{{ item }}"
    state: absent
  loop:
    - "{{ lookup('nginx_paths', want='directories.configuration.base') }}"
    - "{{ lookup('nginx_paths', want='files.configuration') }}"
  loop_control:
    label: "{{ item }}"
