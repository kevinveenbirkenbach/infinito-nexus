- name: "TLS | remove CA trust env vars from {{ FILE_ENVIRONMENT }}"
  become: true
  ansible.builtin.lineinfile:
    path: "{{ FILE_ENVIRONMENT }}"
    regexp: "{{ item }}"
    state: absent
  loop:
    - '^CA_TRUST_CERT_HOST='
    - '^CA_TRUST_WRAPPER_HOST='
    - '^CA_TRUST_NAME='
