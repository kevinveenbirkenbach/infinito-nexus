server:
  domains:
    canonical:
      - "cdn.{{ DOMAIN_PRIMARY }}"
    aliases: []
compose:
  services:
    dashboard:
      enabled: false
    matomo:
      enabled: true
      shared: true
    css:
      enabled: true
