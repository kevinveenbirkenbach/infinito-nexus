features:
  css:     true
server:
  domains:
    canonical:
      - "cdn.{{ DOMAIN_PRIMARY }}"
    aliases: []
docker:
  services:
    desktop:
      enabled: false
      shared: false
    matomo:
      enabled: true
      shared: true
