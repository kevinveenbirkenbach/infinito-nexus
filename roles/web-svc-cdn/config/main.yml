server:
  domains:
    canonical:
      - "cdn.{{ DOMAIN_PRIMARY }}"
    aliases: []
compose:
  services:
    desktop:
      enabled: false
    matomo:
      enabled: true
      shared: true
    css:
      enabled: true
